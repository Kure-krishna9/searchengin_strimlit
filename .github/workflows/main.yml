# name: Sync to Hugging face hub
# on:
#   push:
#     branches: [main]
    
#   # to run this workflow manually from the action tab
#   workflow_dispatch:

# jobs:
#   sync-to-hub:

#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#         with:
#           fetch-depth: 0
#           lfs: true
#       - name: push to hub
#         env:
#           HF_TOKEN: ${{secrets.HF_TOKEN}}
#         run: git push https://Shrikrushnakure:$HF_TOKEN@huggingface.co/spaces/Shrikrushnakure/sk_search_enging


name: Sync to Hugging Face Hub

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true
      - name: Set up Git identity
        run: |
          git config user.name "Kure-krishna9"
          git config user.email "kurekrishna9@gmail.com"

      - name: Sync with Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git remote add hf https://Shrikrushnakure:${HF_TOKEN}@huggingface.co/spaces/Shrikrushnakure/sk_search_enging 
          git pull hf main --rebase
          git push hf main

